/* const submittedForm = document.querySelectorAll('sw_import_form');

submittedForm.addEventListener('submit', submitForAjax ); 

function submitForAjax(event) {
    event.preventDefault();
/*
    const url = submittedForm.getAttribute('action'); 

    const formData = new formData(submittedForm);

    console.log(event); 
/*     fetch(url, {

    })
    .then()
    .then()
    .catch() */
/* } */